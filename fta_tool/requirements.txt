fastapi==0.111.0
uvicorn[standard]==0.29.0
jinja2==3.1.4
# 2.7.4 is the minimum langchain-core (pulled by langgraph) accepts; FastAPI
# 0.111.0 supports pydantic <3, so this bump is safe.
pydantic==2.7.4
sqlalchemy==2.0.30
aiosqlite==0.20.0
python-dotenv==1.0.1
httpx==0.27.0
pyyaml>=6.0
# LangGraph inspect-then-regenerate workflow (Step 2-1).
# Only imported when ENABLE_LANGGRAPH_GENERATION_WORKFLOW=true.
# langchain-core (pulled transitively) requires pydantic>=2.7.4; it is pinned
# here so the resolution is deterministic on Python 3.11 and 3.12.
langgraph==0.2.76
langchain-core==0.3.59
pytest==8.2.0
pytest-asyncio==0.23.6
